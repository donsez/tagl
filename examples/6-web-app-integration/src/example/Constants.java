package example;

/**
 * Constants collection.
 */
public interface Constants {

	/**
	 * The key used to register and retrieve the cron4j scheduler within the
	 * current servlet context.
	 */
	public String SCHEDULER = "cron4j.scheduler";

}
